import template from "./delegateAccess.html";
import controller from "./DelegateAccessController";

let component = {
    template,
    controller,
    controllerAs: "ctrl",
    bindings: {
    }
};

export default component;